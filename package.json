{
  "name": "portfolio-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd portfolio-vamsi && npm run dev",
    "build": "cd portfolio-vamsi && npm run build",
    "start": "cd portfolio-vamsi && npm run start",
    "lint": "cd portfolio-vamsi && npm run lint",
    "install:all": "npm install && cd portfolio-vamsi && npm install"
  }
}
